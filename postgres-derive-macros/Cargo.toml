[package]
name = "postgres-derive-macros"
version = "0.1.0"
authors = ["Steven Fackler <sfackler@gmail.com>"]

[lib]
plugin = true
test = false

[dependencies]
postgres-derive-codegen = { path = "../postgres-derive-codegen" }

[dev-dependencies]
postgres = { git = "https://github.com/sfackler/rust-postgres" }
compiletest_rs = "0.0.11"
